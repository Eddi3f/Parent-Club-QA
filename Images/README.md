# Image assets

- `parent-club-logo.svg` is a local, editable stand-in for the current Squarespace logo. Replace it with the final approved Parent Club logo when available, keeping the same filename to avoid changing page markup.
- `parent-club-antenatal-class.png` is an AI-generated concept image for the home-page hero. Replace it with a licensed Parent Club photograph before launch if preferred.

Future image slots should use descriptive, lowercase, hyphenated filenames and include meaningful alt text in the consuming HTML.